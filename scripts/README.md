# Script Placeholder

The final release should provide simple commands such as:

```bash
python -m src.train --config configs/indian_pines/attention_center_gmlp.yaml
python -m src.evaluate --checkpoint outputs/.../best.pt
```

Add these commands only after they have been tested from a clean environment.
