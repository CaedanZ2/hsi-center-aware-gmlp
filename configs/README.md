# Configuration Placeholder

Create one configuration file per dataset and experiment variant, for example:

```text
configs/
├── indian_pines/
├── pavia_university/
└── salinas/
```

Each configuration should record the split seed, patch size, batch size, optimizer, learning rate, epochs, model dimensions, center-prior parameters, and output directory.
